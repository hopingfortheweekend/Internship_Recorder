# 实习记录工具 · 需求与设计文档

> 版本：v0.1（2026-08-26）｜ 状态：架构已确认，细节待评审
> 本文档是后续开发的唯一参考。任何新想法或变更，先更新本文档，再动代码。

## 1. 项目定位

- 使用者：仅自己（单用户）
- 核心目标：
  1. 有条理地记录实习期间经手的事情，便于日后回忆（功能一，本次实现）
  2. （待评估后决定）借助大模型把记录梳理成简历级的职业表述（功能二）
- 非目标：多用户/团队协作、对外发布、社交分享、原生 App 开发

## 2. 使用场景（已确认）

- **主力设备：手机**，随时随手记录；手机浏览器环境稳定（不清数据、不换浏览器）
- **次要设备：电脑**，阶段性整理（如实习结束更新简历时）
- 记录频率：一天 0~3 条，一天可多条
- 访问要求：手机在任意网络下都能访问 → 排除「电脑当服务器、局域网访问」方案
- 数据诉求：手机与电脑**不需要实时同步**，靠「导出 JSON → 传输到电脑」按需对齐

## 3. 架构决策

### 3.1 选定方案：纯静态前端 + 浏览器本地存储 + 免费静态托管

- 前端：原生 HTML/CSS/JS，无构建工具，无运行时依赖
- 数据：存于手机浏览器 **IndexedDB**，数据永不离开设备、不上传任何服务器
- 托管：部署到 **GitHub Pages**（用户已有 GitHub 账号；源码公开无妨——源码不含任何数据与密钥）
- 国内访问稳定性缓解（用户要求「用起来不卡顿」）：
  1. 资源完全本地化：无外链、无 CDN 依赖（系统字体），页面体积极小，首次加载几乎无感
  2. M3 起内置 service worker 资源缓存：**首次成功打开后，此后打开与使用全程走本地、不依赖网络**（数据本就存在手机 IndexedDB）
- 若实测首次加载仍长期不稳，切换备选托管（Vercel / 腾讯 EdgeOne Pages 等国内可用平台），源码与数据均不受影响
- 部署方式：GitHub 仓库（公开，免费版 Pages 要求）→ Settings → Pages → Deploy from a branch（main / docs）。仓库 https://github.com/hopingfortheweekend/Internship_Recorder，站点 https://hopingfortheweekend.github.io/Internship_Recorder/（只有知道网址的人能访问，勿外传）
- 电脑端：不直接操作网页数据；通过「导出 JSON → 传输到电脑 → Python 脚本解析」完成整理（见 §5.5、§7）

### 3.2 否决过的方案（记录理由，避免日后反复）

| 方案 | 否决理由 |
|---|---|
| 电脑跑 Python 服务，手机局域网访问 | 手机必须与电脑同一网络且电脑开机，不符合「随时随手记」 |
| 公网部署带后端（密码登录） | 复杂度最高，需服务器与鉴权；现阶段无实时同步需求 |
| 电脑为主、手机为辅 | 与用户实际使用场景相反 |

### 3.3 隐私与「不对外公开」分析

- 静态页面本身是公开可访问的，但**页面不含任何数据**：数据只存在于使用者自己浏览器的 IndexedDB 里，别人打开同一网址看到的只是空应用
- 搜索引擎：页面加 `<meta name="robots" content="noindex">`；无外链指向，实际不会被收录
- 唯一需要保密的是「网址」本身，不主动分享即可
- 注意：若将来接入功能二（AI 润色），记录文本会发送给所选模型供应商，属隐私权衡，见 §7.4

### 3.4 风险与缓解

| 风险 | 缓解 |
|---|---|
| 手机清浏览器数据 / 换机导致记录丢失 | ① 醒目的「导出备份」按钮；② 界面定期（如每 30 天）温和提醒导出；③ 导出文件另存到手机本地（文件管理器/网盘） |
| 部分国产 ROM 激进清理浏览器存储 | 同上；PWA 安装后存储更稳（§5.6） |
| 静态托管平台访问不稳定（国内访问 GitHub Pages 时快时慢） | service worker 资源缓存（首次加载后全离线可用）；仍不行则换备选托管（Vercel / 腾讯 EdgeOne Pages 等），切换不影响数据 |
| 托管平台失效 | 前端源码本地留存（建议 git 管理），换平台重新部署即可 |

## 4. 数据模型

### 4.1 记录（Entry）字段

| 字段 | 类型 | 说明 | 必填 |
|---|---|---|---|
| id | string (uuid) | 唯一标识，导出/导入合并的依据 | 自动生成 |
| date | string (YYYY-MM-DD) | 事件日期；表单默认当天，可自由修改（补记允许，未来日期允许但提示） | 是 |
| summary | string | 引导问题 1：一句话概括这件事 | 是（兼作标题） |
| context | string | 引导问题 2：这件事在项目里是哪一环，解决了什么问题？ | 否 |
| blocker | string | 引导问题 3：实践时的卡点是什么？又对此给出了什么样的思路或解决方案？ | 否 |
| outcome | string | 引导问题 4：最后交付了什么，或者说带来了哪些变化？ | 否 |
| project | string | 项目归属；输入时自动补全历史用过的项目名 | 否 |
| created_at | datetime | 创建时间 | 自动 |
| updated_at | datetime | 最后修改时间 | 自动 |

四个引导问题原文（界面按此显示）：

1. 一句话概括这件事
2. 这件事在项目里是哪一环，解决了什么问题？
3. 实践时的卡点是什么？又对此给出了什么样的思路或解决方案？
4. 最后交付了什么，或者说带来了哪些变化？

### 4.2 存储

- 使用 IndexedDB（不用 localStorage：容量大、适合结构化数据、支持索引查询）
- 库名 `internship_recorder`；对象仓库 `entries`（主键 `id`），索引：`date`、`project`、`updated_at`
- 不引入第三方库，用原生 IndexedDB API 封装一个 `store.js` 模块（读写删查 + 全量导出）

### 4.3 导出/导入格式（JSON）

```json
{
  "format": "internship-recorder-backup",
  "version": 1,
  "exported_at": "2026-08-26T12:00:00+08:00",
  "entries": [ "…Entry 数组，字段同 §4.1…" ]
}
```

- 导入合并策略：按 `id` 合并；`id` 已存在时保留 `updated_at` 较新的一方；导入前弹窗告知「将新增 N 条、更新 M 条」
- 电脑端脚本读取同一格式（§7.2、§9）
- 未来若功能二落地、增加新字段（如 `ai_bullets`），`version` 升为 2，导入时做兼容

## 5. 功能需求（功能一）

### 5.1 录入表单

- 入口：全局「+ 新增」按钮
- 日期：日期选择器，默认当天；支持补记
- 四问：多行文本框，第 1 问必填，其余留空可保存
- 项目：文本框 + 历史项目名自动补全（去重列出用过的项目）
- 保存成功后回到列表视图，显示轻提示「已保存」

### 5.2 日历视图

- 月历网格；有记录的日期显示**条数角标**（当天记录条数，最多两位，超过 99 显示「99+」；无记录不显示）
- 点击日期 → 下方展示该日全部记录卡片，按创建时间排序
- 顶部：上月/下月翻页、「今天」快捷按钮
- 点击记录卡片 → 进入详情

### 5.3 列表视图

- 全部记录按日期倒序（同日按创建时间倒序）
- 顶部搜索框：关键词匹配 summary / context / blocker / outcome / project
- 按项目筛选（下拉列出用过的项目）

### 5.4 详情、编辑与删除

- 详情页：完整展示四问 + 日期 + 项目
- 编辑：复用录入表单，预填数据
- 删除：二次确认后删除（不可恢复，文案写清楚）

### 5.5 导出与导入

- 导出：生成 §4.3 格式的 JSON 文件下载，文件名含日期，如 `backup-2026-08-26.json`
- 导入：选择 JSON 文件，校验格式（`format` 字段）后按 §4.3 策略合并
- 手机 → 电脑传输方式：微信「文件传输助手」或网盘（在导出成功提示里顺带提示即可）

### 5.6 （M3，已确认）PWA：安装到主屏幕 + 离线使用

- 说明：PWA 就是把网页「添加到主屏幕」后像 App 一样打开（有图标、无浏览器地址栏），并支持离线使用
- 实现：`manifest.json`（应用名、图标、主题色）+ service worker（资源缓存，见 §3.1），随 M3 部署一并上线
- 安装方式：Android Chrome 首次访问后出现「添加到主屏幕」提示；iOS Safari 用「分享 → 添加到主屏幕」；电脑端 Edge/Chrome 同样可安装到桌面
- 收益：从桌面像 App 一样打开、离线可用、存储更不易被系统清理

## 6. 界面设计原则（简洁）

- **移动端优先**，单列布局；电脑上打开时内容居中、限制最大宽度（约 640px），不单独设计桌面布局
- 主结构：顶部（标题 + 视图切换「日历 / 列表」+「+」新增）；中部（当前视图）；不需要底部导航（仅两个视图）
- 视觉：白色/浅灰背景、单一强调色、卡片式记录条目、行距放宽；中文界面
- 空状态：无记录时显示引导文案（提示点「+」开始记录、说明导出备份入口位置）

## 7. 功能二（AI 梳理）评估 —— 待决策，暂不实现

### 7.1 目标定义

输入一笔记录的四问答案（+ 项目名），输出 1~3 条简历风格要点：

- STAR 风格（情境-任务-行动-结果）
- 动词开头、尽量量化（Q4 答案中如有数字优先保留）
- 每条不超过 2 行

### 7.2 接入方式对比（模型无关，优先兼容 OpenAI 协议——国内主流模型如 DeepSeek、Qwen、GLM、Kimi 等均兼容，切换成本低）

| 方案 | 复杂度 | 说明 |
|---|---|---|
| A. 页面内直连模型 API | 低 | 用户自填 API key（存本机 localStorage）；点「AI 润色」按钮直接调用。前提：所选供应商允许浏览器跨域调用（**实施前先验证 CORS**，部分供应商不开放） |
| B. 电脑端 Python 脚本润色 | 低~中 | 导出 JSON 后在电脑上跑脚本批量润色，key 存电脑配置文件。适合「实习结束集中整理」场景，但失去随手润色体验 |
| C. 免费 Worker 中转 | 中 | 若 A 的 CORS 不通，用 Cloudflare Worker 等做一层转发。需额外部署，key 交给 Worker 平台保管 |

- 提示词本身简单：输入字段固定、输出格式固定，写好一次基本不用调
- **推荐路径**：先不写代码，手动把四问贴给模型试效果 → 满意后做 A → CORS 不通再考虑 C
- 三条路径均不改动功能一的数据结构与界面，风险隔离良好

### 7.3 Token 用量与成本测算（量级估算）

- 口径：中文按 1 字 ≈ 1.5 token 估算（不同模型 tokenizer 有差异）
- 单条输入：四问约 200~600 字（300~900 token）+ 系统提示与格式指令约 500 token（多数模型支持提示缓存复用，实测会低于此数）→ 合计约 0.8k~1.4k token
- 单条输出：1~3 条要点约 150~300 字 → 约 200~450 token
- 规模：每周 5~10 条 ≈ 每月 2 万~8 万 token 量级
- 成本：按国内主流模型价格量级（输入约 0.5~4 元/百万 token，输出 1~16 元/百万 token），**单条成本在几分钱以内**，每周 10 条也仅几毛钱；若选顶级模型，单条约 1 美分量级，仍在「奶茶钱」范围。**实施时以所选供应商官网实时价格为准**
- 结论：成本不构成阻碍，主要权衡在效果与隐私，而非费用

### 7.4 待决策项（决定做功能二时再回答）

1. 用哪家模型（建议优先 OpenAI 兼容协议的国内模型）
2. 采用 §7.2 哪种接入方式
3. 隐私权衡是否接受（记录文本会发送给模型供应商）
4. 润色结果是否回写为记录的新字段（如 `ai_bullets`），还是仅在导出/整理时生成

## 8. 开发计划（里程碑）

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| M0 骨架 | 静态页 + store 模块（IndexedDB 读写）+ 录入表单 | 新增记录后刷新页面，数据仍在 |
| M1 核心视图 | 日历视图 + 列表视图 + 搜索/项目筛选 + 详情/编辑/删除 | 日历打点正确；点击日期看到当日多条；搜索能命中正文；删除有确认 |
| M2 数据安全 | 导出/导入 + 备份提醒 | 导出文件可被重新导入且不产生重复；合并策略符合 §4.3 |
| M3 上线 | 部署到静态托管 + noindex + PWA（资源缓存 + 安装到主屏幕）+ 手机实测 | 手机浏览器正常使用；断网可用；可添加到主屏幕像 App 打开；搜索引擎搜不到；导出导入在手机端可用 |
| M4 整理脚本 | tools/export_to_markdown.py：读取导出 JSON，生成按项目分组的 Markdown 简历草稿 | 手机导出 JSON 传到电脑，运行脚本生成分组文档 |
| M5 待定 | 功能二（先按 §7.4 决策） | — |

建议每完成一个里程碑用真实数据（自己录几条）验收一次。

## 9. 目录结构建议

```
internship_recorder/
├─ 设计文档.md             ← 本文档
├─ docs/                   ← 前端，即 GitHub Pages 部署目录（原 app/）
│  ├─ index.html
│  ├─ style.css
│  ├─ manifest.json        # PWA 安装清单（名称/图标）
│  ├─ sw.js                # service worker 资源缓存（离线可用）
│  ├─ icon-192.png / icon-512.png / icon-maskable-512.png
│  └─ js/
│     ├─ store.js          # IndexedDB 封装（增删改查 + 导出导入）
│     ├─ views.js          # 日历/列表/表单渲染
│     └─ main.js           # 入口与状态管理
├─ tools/                  ← 电脑端辅助脚本
│  ├─ generate_icons.py    # 生成 PWA 图标（纯标准库）
│  └─ export_to_markdown.py  # JSON → 按项目分组的 Markdown 简历草稿（M4）
└─ main.py                 ← 待迁入 tools/（M4 并入整理脚本）
```

## 10. 决策记录（2026-08-26 已确认）

1. 托管平台：GitHub Pages（已有 GitHub 账号）；稳定性靠资源本地化 + service worker 缓存缓解，实测不稳再换国内备选（§3.1）
2. 日历标记：条数角标，数字最多两位，超过 99 显示「99+」（§5.2）
3. PWA：确认纳入一期（M3），含安装到主屏幕 + 离线使用（§5.6）
4. 电脑端整理脚本（tools/export_to_markdown.py）：保留，排入 M4
5. GitHub 仓库公开 + Pages 部署目录 /docs（M3）：仓库 Internship_Recorder，站点 https://hopingfortheweekend.github.io/Internship_Recorder/（§3.1）
6. git 提交身份：远棹轻歌 / hopingfortheweekend@users.noreply.github.com（GitHub 隐私邮箱）
